{
  "name": "richtext-visual-api",
  "version": "1.0.0",
  "description": "Azure Function API for the Rich Text Power BI visual",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "dev": "npm run build && func start"
  },
  "dependencies": {
    "mssql": "^9.1.4"
  },
  "devDependencies": {
    "@azure/functions": "^3.5.0",
    "@types/node": "^18.0.0",
    "typescript": "^4.9.5"
  }
}
